# Reading Improving / LReading Agent 协作规则

<!-- LR-ANCHOR:AI-ENTRY -->

本文件是任何人类协作者、Hermes、Codex、Claude、GPT-5.4 mini 或其他 Agent 的执行入口。**先读本文档，再行动。**

## 0. 当前事实与禁止事项

- 项目当前仅有文档骨架，`LR-PHASE-00` 已完成；没有业务实现、依赖或构建产物。
- 当前只允许执行 [docs/CONSTRUCTION_PLAN.md](docs/CONSTRUCTION_PLAN.md) 中被用户明确授权的**一个**阶段。
- `docs/STATUS.md` 是唯一状态事实来源。不得因“计划看起来完整”而把未执行阶段写成完成。
- 未得到用户授权时，禁止：安装依赖、写业务代码、改 Obsidian Vault、上传 EPUB、调用模型 API、批量修改用户 Markdown。
- 用户中断命令或要求“先看方案”时，立即停止，不要重试同一个动作。

## 1. 必读顺序（每次新会话、每个阶段都执行）

1. `README.md`
2. `AGENTS.md`（本文件）
3. `docs/STATUS.md`
4. `docs/CONSTRUCTION_PLAN.md` 中目标 `LR-PHASE-NN` 和对应 `LR-CHECK-NN`
5. `docs/ARCHITECTURE.md` 中该阶段引用的 `LR-DECISION-*` / `LR-RISK-*`
6. `docs/contracts/README.md` 及被该阶段列出的具体契约
7. 若写生产代码：加载并遵守 `test-driven-development` skill；若为 Obsidian 插件：加载并遵守 `obsidian-plugin-development` skill。

如果任何必读文件缺失、互相矛盾，或账本显示 `blocked`，停止实现，报告证据和最小修复建议。

## 2. 一阶段施工协议

### 开始条件

只有同时满足下列条件，才可开工：

- 用户明确授权目标阶段，例如“执行 LR-PHASE-03”。
- `STATUS.md` 中该阶段为 `ready`，前置阶段为 `done`（或计划明确允许并行）。
- 已阅读目标阶段的全部文件范围、任务列表、停止点和验收命令。
- 无未解释的工作区改动；若有，先执行只读 `git status --short` 并报告。

### 全量施工授权例外

默认规则是“一次只施工一个阶段”。但用户在 2026-08-17 明确授权：**已确认的核心 MVP 阶段连续执行全部步骤，完成后再统一复核。**

因此，可连续施工 `LR-PHASE-01` 至 `LR-PHASE-10`，但每个阶段仍必须：完成本阶段所有 Task、运行对应 `LR-CHECK-NN`、更新 `STATUS.md`、提交真实结果，才可进入下一阶段。以下边界不因全量授权失效：

- `LR-PHASE-11` 仍是 `deferred`，必须单独确认模型供应商、隐私范围、费用预算及写入规则；
- 遇到缺失的真实输入、不可逆迁移、外部账号/付费、危险删除或验证失败时，停止并报告阻塞证据；
- 不得为了“连续施工”跳过 TDD、验收、部署验证或用户数据保护。

### 固定执行循环

1. 将该阶段改为 `in_progress`，并在 STATUS 新增“开始记录”。
2. 严格按计划的 **Task 编号顺序** 操作；一次只做一个最小行为。
3. 每个生产行为遵循 TDD：先写一个失败测试 → 运行并确认因功能缺失而失败 → 写最小实现 → 运行目标测试 → 运行阶段要求的全量测试。
4. 不为后续阶段预写“顺手的”功能；计划没允许的文件不修改。
5. 运行 `LR-CHECK-NN` 中所有自动化命令，记录真实退出码与关键输出。
6. 更新 STATUS：改动文件、测试结果、偏离、风险、需用户手工验收项、下一阶段建议。
7. 若自动化通过但需 Obsidian 真实界面验证，标记为 `review`；否则标记为 `done`。
8. 停止，等待用户下一次授权；不得自动进入后续阶段。

## 3. 低上下文 / 低能力 Agent 的任务纪律

本项目的任务被刻意拆小。执行 Agent 必须：

- 每次只声称完成一个 `Task NN.X`，不可把相邻任务合并为“差不多做完”。
- 开始 Task 前复述：**输入、允许改动文件、预期测试、停止条件**。
- 不理解术语时，先打开计划引用的契约；不要根据文件名猜测数据结构。
- 不能运行的命令要报告原始错误、环境信息和替代路径；不得伪造绿色测试结果。
- 不得从网络拷贝未经审查的 EPUB 解析代码、样书内容、API Key 或版权文本。
- 不得删除文件以“让测试通过”；除非计划/用户明确允许删除且已证明确无引用。
- 任何数据迁移、重导入、批量标签、正式笔记写入都属于用户资产写操作：先展示影响范围和预览，再确认。

## 4. 不变量（所有阶段均不可破坏）

<!-- LR-ANCHOR:INVARIANTS -->

1. **转换不用大模型**：EPUB 解包、解析、章节化、资源导出、Markdown 渲染、锚点生成和目录渲染必须是确定性代码。
2. **Markdown 是用户资产**：用户记录在 `阅读笔记与摘要.md`；插件不得静默改写用户正文。
3. **定位必须可验证**：任何笔记 `sourceRefs` 必须由系统从已导入章节和段落块 ID 组装；模型或用户输入不能伪造系统定位字段。
4. **不依赖 EPUB 页码**：禁止把阅读位置设计成设备相关页码或裸字符偏移。
5. **Vault 路径是虚拟相对路径**：生产存储层仅使用相对 Vault 路径和 `/` 分隔符；禁止将 Obsidian Vault API 路径当作主机绝对路径。
6. **用户数据与派生数据隔离**：导入生成的 `chapters/` 与 `assets/` 可以重建；`阅读笔记与摘要.md` 是用户数据，不得被重导入覆盖。
7. **书籍标签默认不复制进每章**：书籍级 `tags` 写 `书籍.md`；章节/笔记用 `book: "[[书籍]]"` 表达归属。实际批量物化标签须是独立、可预览的可选命令。
8. **没有 `数据摘要.md`**：不得创建、引用或以它作为统计数据来源。可重建统计只能由索引和资料目录 UI 显示。
9. **EPUB 内容视为不可信输入**：不执行脚本，不解析外部网络资源，不允许 ZIP 路径穿越、解压炸弹或覆盖 Vault 范围外文件。
10. **重导入不伪造成功**：无法可靠保留的旧回链必须标记需复核，绝不能静默指向错误段落。
11. **未来 AI 只能草拟**：任何 AI 生成摘要、分类、时间线、概念或笔记都必须带来源、预览与用户确认。

## 5. 文件所有权

| 路径/类型 | 所有者 | Agent 可否覆盖 |
|---|---|---|
| `docs/` | 项目施工文档 | 仅在当前阶段计划允许且同步 STATUS 时可改 |
| `src/`、`tests/` | 插件代码与测试 | 仅当前阶段允许范围；生产代码必须有先失败测试 |
| Vault `LReading/Books/*/chapters/` | 导入器派生正文 | 仅重导入流程、预览和安全检查后可改 |
| Vault `LReading/Books/*/assets/` | 导入器派生资源 | 同上 |
| Vault `书籍.md`、`目录.md`、`书架.md` | 插件生成入口 | 只更新明确标记的系统区块；用户可编辑区必须保留 |
| Vault `阅读笔记与摘要.md` | 用户 | 只能通过用户明确动作追加；不得批量覆写或重导入改写 |
| 原 EPUB / 用户 Vault / API Key | 用户私有资产 | 不提交源码仓库；不写日志；不在测试 fixture 中保留真实内容 |

## 6. 测试、提交与验证

- 生产代码一律遵循 RED → GREEN → REFACTOR；测试首次必须实际失败，不能只补“已经通过”的测试。
- 单元测试应覆盖纯函数和边界：路径、目录、解析、锚点、标签合并、重导入决策、笔记追加。
- Obsidian 运行时依赖必须隔离在 adapter/UI 层，纯领域模块不可依赖 `obsidian` 包。
- 每阶段至少运行计划列出的 `npm run lint`、`npm run typecheck`、`npm test`、`npm run build`（这些命令在 PHASE-01 才会创建）。
- 手工验收只能标注“待用户确认”或“已由用户确认”，不得用 Agent 截图或推测替代。
- 提交须按阶段拆分，中文约定式提交；提交 body 写真实验证结果和遗留项。推送/PR 仅在用户授权时进行。

## 7. 交接模板

阶段结束时在 `STATUS.md` 写入：

```markdown
### YYYY-MM-DD — LR-PHASE-NN 交接
- 状态：done | review | blocked
- 完成 Task：NN.1、NN.2
- 修改：`path/a`（原因）；`path/b`（原因）
- 自动验证：`命令` → 退出码 / 关键结果
- 手工验证：已完成 / 待用户确认（具体步骤）
- 偏离：无 / 说明与理由
- 风险与未决项：...
- 下一建议：仅建议 `LR-PHASE-NN+1`，不自动开工。
```

## 8. 当计划需要变更时

需求改变时，先修改 `ARCHITECTURE.md` 的决策/风险和 `CONSTRUCTION_PLAN.md` 的相应阶段；再修改 `STATUS.md` 摘要与账本。历史交接记录不重写。新增范围必须有新的锚点与验收，而不是藏进旧任务。